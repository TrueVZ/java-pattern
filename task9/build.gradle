plugins {
    id 'java'
}

group 'ru.mirea'
version '1.0'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes 'Main-Class': 'ru.mirea.Main'
    }

    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }

    finalizedBy{
        copyJarToRoot
    }

}

task copyJarToRoot(type: Copy){
    from jar
    into ('dockerBuild')
    finalizedBy{
        createDocker
    }
}

task createDocker(type: Exec){
    workingDir 'dockerBuild'
    commandLine "docker", "build", "-t", "task9:1.0", "."
    finalizedBy{
        runDocker
    }
}

task runDocker(type: Exec){
    commandLine "docker", "run", "task9:1.0"
}
